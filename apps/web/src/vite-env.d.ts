/// <reference types="vite/client" />

/**
 * @fileoverview Vite client env typings for the web app.
 *
 * **What:** Declares `import.meta.env` shape used by `lib/api`.
 * **Why:** Keeps `VITE_API_URL` typed without ambient `any`.
 */
